// we will use .at(index) to find value on particular index of array

//firstly we get element by using square brackeut syntax [] directly with the index starting from 0

const users = ["Sam", "Alex", "Charley"];
console.log)(users[1]);

//we can also use .at(index) to get the value of an array
console.log(users.at(1)); //"Alex"
console.log(users.at(-2)); //"Alex"

