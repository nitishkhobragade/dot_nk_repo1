//you can add element to the end of an array using .push(element) method.

const numbers = [10, 8, 13, 15];
console.log(numbers.length + "  length of array before pushing");
//pushing element to the end of an array
numbers.push(20);
console.log(numbers.length + "  length of array after pushing");
console.log(numbers);
